import ProductsList from "../Components/ProductsList";

function Products() {

    return (
        <div className="cards">
            <ProductsList/>
        </div>
    );
}

export default Products;
